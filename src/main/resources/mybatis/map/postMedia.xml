<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="PostMediaMapper">

  <insert id="save" parameterType="PostMedia" keyProperty="id" useGeneratedKeys="true">
    INSERT INTO posts_media (post_id, url, created_id, created_at)
    VALUES (#{postId}, #{url}, #{createdId}, #{createdAt})
  </insert>

</mapper>