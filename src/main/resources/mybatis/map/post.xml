<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="PostMapper">

  <insert id="save" parameterType="Post" keyProperty="id" useGeneratedKeys="true">
    INSERT INTO posts (member_id, status, created_id, created_at)
    VALUES (#{memberId}, #{status}, #{createdId}, #{createdAt})
  </insert>

  <update id="update" parameterType="Post">
    UPDATE posts
    SET
    post_type = #{postType},
    title = #{title},
    content = #{content},
    status = #{status},
    created_id = #{createdId},
    created_at = #{createdAt}
    WHERE id = #{id}
  </update>

  <select id="findById" parameterType="Long" resultType="post">
    SELECT * FROM posts WHERE id = #{id};
  </select>

</mapper>